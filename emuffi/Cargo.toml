[package]
name = "emuffi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "模拟器核心的 C ABI 导出层"

[lib]
name = "nieche"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
cbelib = { path = "../cbelib" }
emucore = { path = "../emucore" }
